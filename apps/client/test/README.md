# Client tests (placeholder)

Tests will be added here. When ready:

- Add `"test": "vitest run"` to `apps/client/package.json` scripts.
- Add Vitest config and test files under this folder.
- CI already runs `pnpm turbo run test` from root; admin runs first, client will run once this app has a `test` script.
